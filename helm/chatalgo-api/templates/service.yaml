apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatalgo-api.fullname" . }}
  labels:
    {{- include "chatalgo-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatalgo-api.selectorLabels" . | nindent 4 }}
